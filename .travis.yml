language: cpp
compiler:
  - gcc
  - clang
before_install:
 - sudo apt-get update -qq
 - sudo apt-get upgrade -qq
 - sudo apt-get install -qq unixodbc-dev libmysqlclient-dev
script: ./configure & make -s

